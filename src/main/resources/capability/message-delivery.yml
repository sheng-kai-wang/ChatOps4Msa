# 被動接收訊息


info:
  version: 1.0.0
  title: Message Delivery
  description: |
    Using RabbitMQ to receive message

low_code:
  on_message:
    - name: receive-rabbitmq-service_code_scanning
      parameter:
        event: null
      description: |
        Trigger when code scanning is ok.
      access: private
      body:
        - toolkit-discord-text:
            text: ${event}